export type Config = {
	ownerID: string,
	defaultPrefix: string,
	token: string,
	devToken: string,
	anilist:
	{
		client_id: string,
		client_secret: string,
	},
	googleToken: string,
	ritoToken: string,
	logLevel: string,
};
