export type Config = {
	anilist:
	{
		client_id: string,
		client_secret: string,
	},
	database: string,
	defaultPrefix: string,
	devToken: string,
	googleToken: string,
	logLevel: string,
	ownerID: string,
	ritoToken: string,
	token: string,
};
